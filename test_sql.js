const db = require('./src/db'); db.query('SELECT p.serial_number, c.first_name, c.last_name, c.email, pr.name AS program_name FROM customer_passes p JOIN customers c ON c.id = p.customer_id JOIN loyalty_programs pr ON pr.id = p.program_id WHERE p.tenant_id = \ AND NOT c.anonymized AND (c.first_name ILIKE \ OR c.last_name ILIKE \ OR c.email ILIKE \ OR c.phone ILIKE \) LIMIT 10', ['00000000-0000-0000-0000-000000000000', '%test%']).then(console.log).catch(e => console.error(e.message)).finally(() => process.exit(0));
